# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Stripe Configuration
# Get these from: https://dashboard.stripe.com/test/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...

# Stripe Webhook Secret
# Generate with: stripe listen --forward-to localhost:3000/api/stripe/webhooks --print-secret
STRIPE_WEBHOOK_SECRET=whsec_...

# Instructions:
# 1. Copy this file to .env.local
# 2. Fill in your actual values from Supabase and Stripe
# 3. Never commit .env.local to version control
# 4. For Stripe webhook testing, run: stripe listen --forward-to localhost:3000/api/stripe/webhooks --print-secret
